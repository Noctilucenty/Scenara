// Single source of truth for the app version string shown in Settings.
// Update this file in the same commit as a new git tag (vX.Y.Z) and the
// version display will follow automatically.
export const APP_VERSION = "0.9.9b1";
